Options -Indexes

# Require password for admin (optional — use PHP auth instead)
# <FilesMatch "\.php$">
#   AuthType Basic
#   AuthName "Admin Area"
#   AuthUserFile /path/to/.htpasswd
#   Require valid-user
# </FilesMatch>

# Block direct access to non-PHP files
<FilesMatch "\.(log|bak|sql|md|env)$">
    Order allow,deny
    Deny from all
</FilesMatch>
